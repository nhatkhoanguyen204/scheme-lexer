# How to run

## Building the repo

Go to terminal in this folder and run:
```
cmake -S . -B build
cmake --build build
./build/lexer <a Scheme file>
```
and you should be able to receive the output as intended.